<%= form_with(model: unit_module, local: true) do |form| %>
  <% if unit_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(unit_module.errors.count, "error") %> prohibited this unit_module from being saved:</h2>

      <ul>
      <% unit_module.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= form.label :title %>
    <%= form.text_field :title, class: "form-control"%>
  </div>

  <div class="form-group">
    <%= form.label :description %>
    <%= form.text_area :description , class: "form-control"%>
  </div>

  <div class = "form-group">
      <%= form.label :image%>
      <%= form.file_field :image, class: "form-control-file"%>
  </div>

  <div class = "form-group">
      <%= form.label :files%>
      <%= form.file_field :files, multiple: true, class: "form-control-file" %>
  </div>
  <div class="actions">
    <%= form.submit "Submit", class: "btn btn-primary" %>
  </div>
<% end %>
